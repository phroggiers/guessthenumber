apiVersion: apps/v1
kind: Deployment
metadata:
  name: guessthenumber-py
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: click-py
  template:
    metadata:
      labels:
        app: guessthenumber-py
    spec:
      containers:
        - name: guessthenumber-py
          image: python:latest
          ports:
            - containerPort: 80
